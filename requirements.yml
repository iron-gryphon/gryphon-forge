---
# Ansible Galaxy collections required by Gryphon Forge
collections:
  - name: amazon.aws
    version: ">=7.0.0"
  - name: community.aws
    version: ">=4.0.0"
  - name: kubernetes.core
    version: ">=4.0.0"
  - name: community.general
    version: ">=8.0.0"
